apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "device-platform.fullname" . }}-test-connection"
  labels:
    {{- include "device-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "device-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
