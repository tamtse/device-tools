apiVersion: v1
kind: Service
metadata:
  name: {{ include "device-platform.fullname" . }}
  labels:
    {{- include "device-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "device-platform.selectorLabels" . | nindent 4 }}
