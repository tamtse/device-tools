apiVersion: v1
kind: ConfigMap
metadata:
  name: statistic-api-config
  namespace: device-platform
data:
  LOG_LEVEL: INFO
  REQUEST_TIMEOUT_SECONDS: "5"
  DATABASE_URL: postgresql://devices:user@postgres:5432/devices
  DEVICE_REGISTRATION_API_URL: http://device-registration-api:8000